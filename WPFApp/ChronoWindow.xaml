<Window x:Class="WPFApp.ChronoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFApp"
        mc:Ignorable="d"
        Title="Chronomètre" Height="400" Width="400" WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <local:ChronoViewModel/>
    </Window.DataContext>

    <Grid>
        <Ellipse Stroke="Black" StrokeThickness="3" Width="250" Height="250" HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <Rectangle Width="4" Height="100" Fill="PaleVioletRed" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,1" Margin="0,0,0,100">
            <Rectangle.RenderTransform>
                <RotateTransform Angle="{Binding SecondAngle}"/>
            </Rectangle.RenderTransform>
        </Rectangle>

        <Rectangle Width="4" Height="70" Fill="DarkBlue" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,1" Margin="0,0,0,70">
            <Rectangle.RenderTransform>
                <RotateTransform Angle="{Binding MinuteAngle}"/>
            </Rectangle.RenderTransform>
        </Rectangle>

        <!-- Nombres sur le cadran -->
        <Canvas Width="250" Height="250"
                HorizontalAlignment="Center" VerticalAlignment="Center">

            <!-- On place les nombres manuellement autour du cercle -->
            <TextBlock Text="60" FontSize="14" FontWeight="Bold" Canvas.Left="117" Canvas.Top="5"/>
            <TextBlock Text="5"  FontSize="14" FontWeight="Bold" Canvas.Left="180" Canvas.Top="20"/>
            <TextBlock Text="10" FontSize="14" FontWeight="Bold" Canvas.Left="215" Canvas.Top="60"/>
            <TextBlock Text="15" FontSize="14" FontWeight="Bold" Canvas.Left="230" Canvas.Top="115"/>
            <TextBlock Text="20" FontSize="14" FontWeight="Bold" Canvas.Left="215" Canvas.Top="170"/>
            <TextBlock Text="25" FontSize="14" FontWeight="Bold" Canvas.Left="175" Canvas.Top="210"/>
            <TextBlock Text="30" FontSize="14" FontWeight="Bold" Canvas.Left="117" Canvas.Top="225"/>
            <TextBlock Text="35" FontSize="14" FontWeight="Bold" Canvas.Left="60" Canvas.Top="210"/>
            <TextBlock Text="40" FontSize="14" FontWeight="Bold" Canvas.Left="20" Canvas.Top="170"/>
            <TextBlock Text="45" FontSize="14" FontWeight="Bold" Canvas.Left="5"  Canvas.Top="115"/>
            <TextBlock Text="50" FontSize="14" FontWeight="Bold" Canvas.Left="20" Canvas.Top="60"/>
            <TextBlock Text="55" FontSize="14" FontWeight="Bold" Canvas.Left="60" Canvas.Top="20"/>

        </Canvas>

        <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Center" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="Start" Command="{Binding StartCommand}" Width="100"/>
            <Button Content="Stop" Command="{Binding StopCommand}" Width="100"/>
            <Button Content="Reset" Command="{Binding ResetCommand}" Width="100"/>
        </StackPanel>
    </Grid>
</Window>
